<view class="personal_detail">
  <block wx:for="{{personalMessage}}">
    <view class="personal_detail-message">
      <view class='personal_detail-left'>{{item.name}}</view>
      <view class='personal_detail-right'>{{item.tel}}</view>
    </view>
  </block>
</view>